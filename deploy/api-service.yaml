apiVersion: v1
kind: Service
metadata:
  name: task-manager-api
  labels:
    app: task-manager
    component: api
spec:
  type: ClusterIP
  selector:
    app: task-manager
    component: api
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
