apiVersion: v1
kind: Service
metadata:
  name: task-manager
  labels:
    app: task-manager
spec:
  type: NodePort
  selector:
    app: task-manager
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30080
      protocol: TCP
